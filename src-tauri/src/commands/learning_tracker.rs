// V4// V4.3 — 学习进度追踪 (Learning Progress// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
//// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
//// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRad// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// -// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recomm// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize,// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ===========================================================================// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ===========================================================================// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState>// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// =// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// =========================================================================// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
    pub skills: HashMap<String, f64>,
// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
    pub skills: HashMap<String, f64>,
    /// 已完成的实验 ID 列表
// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
    pub skills: HashMap<String, f64>,
    /// 已完成的实验 ID 列表
    pub completed_experiments: Vec<String>,
// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
    pub skills: HashMap<String, f64>,
    /// 已完成的实验 ID 列表
    pub completed_experiments: Vec<String>,
    /// 所有实验总分平均值
    pub total// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
    pub skills: HashMap<String, f64>,
    /// 已完成的实验 ID 列表
    pub completed_experiments: Vec<String>,
    /// 所有实验总分平均值
    pub total_score_avg: f64,
    ///// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
    pub skills: HashMap<String, f64>,
    /// 已完成的实验 ID 列表
    pub completed_experiments: Vec<String>,
    /// 所有实验总分平均值
    pub total_score_avg: f64,
    /// 强项技能列表 (>70 分)
// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
    pub skills: HashMap<String, f64>,
    /// 已完成的实验 ID 列表
    pub completed_experiments: Vec<String>,
    /// 所有实验总分平均值
    pub total_score_avg: f64,
    /// 强项技能列表 (>70 分)
    pub strengths: Vec<String>,
    /// 弱// V4.3 — 学习进度追踪 (Learning Progress Tracker)
//
// 核心功能:
// 1. 学生技能雷达图数据生成 — 六大维度量化评估
// 2. 学习进度汇总 — 实验记录、平均分、强弱项分析
// 3. 实验结果记录 — 持久化到内存 HashMap
// 4. 个性化练习推荐 — 针对弱项生成 3 个推荐实验
//
// 集成点:
// - 前端技能雷达图组件 (SkillRadarData)
// - 实验平台结果提交 (record_experiment_result)
// - 自适应学习路径推荐 (get_practice_recommendations)

use serde::{Deserialize, Serialize};
use std::collections::HashMap;
use std::sync::Mutex;
use tauri::command;
use tracing::info;

use lazy_static::lazy_static;
use uuid::Uuid;

// ============================================================================
// 全局状态
// ============================================================================

lazy_static! {
    static ref LEARNING_STATE: Mutex<LearningState> = Mutex::new(LearningState::default());
}

#[derive(Debug, Default)]
pub struct LearningState {
    pub profiles: HashMap<String, SkillProfile>,
    pub records: HashMap<String, Vec<ExperimentRecord>>,
}

// ============================================================================
// 数据结构
// ============================================================================

/// 学生技能画像
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct SkillProfile {
    pub student_id: String,
    /// 技能维度 → 平均分 (0-100)
    pub skills: HashMap<String, f64>,
    /// 已完成的实验 ID 列表
    pub completed_experiments: Vec<String>,
    /// 所有实验总分平均值
    pub total_score_avg: f64,
    /// 强项技能列表 (>70 分)
    pub strengths: Vec<String>,
    /// 弱项技能列表 (<40 分)
    pub