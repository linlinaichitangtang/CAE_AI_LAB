# CAELab V1.0.2 UI 补全任务计划

## 任务目标
根据 `/Volumes/ZHITAI T iPlus7100 /cae/docs/trae_ui_tasks_prompt.md` 的要求，完成 Phase 2 / Phase 3 UI 遗留任务，共 7 项。

## 当前代码状态分析
- **SimulationView.vue**：已包含基础结构，分析类型下拉菜单已扩展，材料和边界条件面板已部分重设计，右侧面板宽度已统一为 w-72
- **需要补全**：新分析类型的参数表单、后处理全屏模式、图表联动、动效优化

## 任务计划

### 任务一：仿真模块标签页化补全
1. **扩展分析类型参数表单**
   - 在 SimulationView.vue 中添加 transient 分析的参数表单（时间步设置、加载曲线、Newmark-beta 参数）
   - 添加 fatigue 分析的参数表单（高周/低周选择、S-N曲线、应力比、寿命计算）
   - 添加 thermal_coupling 分析的参数表单（顺序耦合/完全耦合、温度场→结构场映射）

### 任务二：后处理全屏 + 图表联动
1. **全屏模式**
   - 在求解完成区域添加"全屏查看"按钮
   - 实现全屏切换逻辑
2. **图表联动**
   - 实现点击曲线上的点 → 3D 视图中高亮对应位置的节点
   - 实现拖动时间轴滑块 → 3D 云图同步更新
   - 添加"分离/联动"切换按钮

### 任务三：材料/边界条件面板重设计
1. **完善材料面板**
   - 确保所有分析类型的材料面板都采用 COMSOL 风格
   - 为所有材料参数添加单位下拉菜单
2. **完善边界条件面板**
   - 确保所有分析类型的边界条件面板都采用折叠式设计
   - 统一边界条件分类和布局

### 任务四：右侧参数面板规范化
- **验证宽度一致性**：确保所有右侧面板宽度统一为 w-72
- **验证样式一致性**：确保面板背景、边框、内边距等样式统一

### 任务五：动效和过渡优化
1. **添加统一 transition 类**
   - 在 src/styles/main.css 中添加 transition-panel 和 transition-expand 类
2. **实现标签切换动画**
   - 为 activeTab 切换添加滑动下划线指示器
3. **添加加载骨架屏**
   - 为求解进行中状态添加骨架屏（shimmer 动画）

## 技术约束
- 不破坏现有功能
- 不修改 Rust 命令
- 确保编译通过
- 确保移动端兼容

## 交付标准
1. `analysisType` 包含全部 9 种分析类型
2. 瞬态/疲劳/热耦合三种分析的参数表单内嵌在 SimulationView.vue 右侧面板
3. 后处理全屏模式可切换
4. 材料面板符合 COMSOL 风格（有单位下拉）
5. 右侧面板宽度统一 w-72
6. `git commit` 提交，commit message 以 `feat(ui):` 开头

## 实施步骤
1. 首先完成任务一：添加新分析类型的参数表单
2. 然后完成任务二：实现后处理全屏和图表联动
3. 接着完成任务三：完善材料和边界条件面板
4. 然后完成任务五：添加动效和过渡优化
5. 最后验证任务四：确保右侧面板规范化
6. 运行测试确保编译通过
7. 提交代码